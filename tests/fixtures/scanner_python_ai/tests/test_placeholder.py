"""Placeholder test for the scanner_python_ai fixture.

This test doesn't execute any real logic — the fixture exists so the
scanner can find a `tests/` directory and flag pytest as a detected runner.
"""


def test_placeholder() -> None:
    assert True
